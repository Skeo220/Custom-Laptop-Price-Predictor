library(tidyverse)


# Global variables, packages, or data can be defined here

input_cols <- c(
  "product_type", "screen_size", "resolution", "display", "touch_screen", "cpu",
  "processor_speed", "ram", "drive_capacity", "drive_type", "extra_drive_capacity",
  "gpu", "op_sys", "weight"
)
